@model DashboardViewModel
@{
    ViewData["Title"] = "Dashboard";
}

<div class="container-fluid">
    <!-- Excel Butonu ve Arama Kutusu - MOBILE OPTIMIZED -->
    <div class="row mb-3">
        <div class="col-md-6 mb-2 mb-md-0">
            <div class="d-flex flex-column flex-md-row gap-2">
                <a href="@Url.Action("ExportDashboard", "Excel")" class="btn btn-success flex-fill text-center">
                    <i class="bi bi-file-earmark-excel"></i> Excel'e Aktar
                </a>
                <a href="@Url.Action("Index", "Rapor")" class="btn btn-info flex-fill text-center">
                    <i class="bi bi-graph-up"></i> Raporlar
                </a>
            </div>
        </div>
        <div class="col-md-6">
            <form method="get" class="d-flex flex-column flex-md-row gap-2">
                <input type="text" name="search" class="form-control flex-fill"
                       placeholder="🔍 Sipariş No veya Ürün Adı ara..."
                       value="@Context.Request.Query["search"]">
                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-primary flex-fill">Ara</button>
                    @if (!string.IsNullOrEmpty(Context.Request.Query["search"]))
                    {
                        <a href="@Url.Action("Index")" class="btn btn-secondary flex-fill">Temizle</a>
                    }
                </div>
            </form>
        </div>
    </div>

    <!-- Hoş Geldiniz Mesajı - MOBILE OPTIMIZED -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="alert alert-success">
                <div class="d-flex flex-column flex-md-row justify-content-between align-items-start align-items-md-center">
                    <div class="mb-2 mb-md-0">
                        <h4 class="mb-1">🎉 Hoş geldiniz, @Context.Session.GetString("AdSoyad")!</h4>
                        <p class="mb-0">Sistem durumu: <span class="badge bg-success">Aktif</span> | Rolünüz: <span class="badge bg-info">@Context.Session.GetString("Role")</span></p>
                    </div>
                    <div class="text-md-end">
                        <small class="text-muted">@DateTime.Now.ToString("dd.MM.yyyy HH:mm")</small>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- İstatistik Kartları - MOBILE OPTIMIZED -->
    <div class="row mb-4">
        <div class="col-6 col-sm-4 col-md-3 col-xl-2 mb-3">
            <div class="card text-white bg-danger h-100">
                <div class="card-body p-2 p-sm-3">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <div class="text-white-50 small">Makine Saat</div>
                            <div class="fs-5 fs-md-4 fw-semibold">@Model.ToplamMakineSaat</div>
                        </div>
                        <div class="fs-3 fs-md-2">🏭</div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-6 col-sm-4 col-md-3 col-xl-2 mb-3">
            <div class="card text-white bg-warning h-100">
                <div class="card-body p-2 p-sm-3">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <div class="text-white-50 small">Ekleme</div>
                            <div class="fs-5 fs-md-4 fw-semibold">@Model.ToplamEkleme</div>
                        </div>
                        <div class="fs-3 fs-md-2">🔧</div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-6 col-sm-4 col-md-3 col-xl-2 mb-3">
            <div class="card text-white bg-info h-100">
                <div class="card-body p-2 p-sm-3">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <div class="text-white-50 small">Dazmal</div>
                            <div class="fs-5 fs-md-4 fw-semibold">@Model.ToplamDazmal</div>
                        </div>
                        <div class="fs-3 fs-md-2">⚡</div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-6 col-sm-4 col-md-3 col-xl-2 mb-3">
            <div class="card text-white bg-primary h-100">
                <div class="card-body p-2 p-sm-3">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <div class="text-white-50 small">Kesme</div>
                            <div class="fs-5 fs-md-4 fw-semibold">@Model.ToplamKesme</div>
                        </div>
                        <div class="fs-3 fs-md-2">✂️</div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-6 col-sm-4 col-md-3 col-xl-2 mb-3">
            <div class="card text-white bg-success h-100">
                <div class="card-body p-2 p-sm-3">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <div class="text-white-50 small">Paketleme</div>
                            <div class="fs-5 fs-md-4 fw-semibold">@Model.ToplamPaketleme</div>
                        </div>
                        <div class="fs-3 fs-md-2">📦</div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-6 col-sm-4 col-md-3 col-xl-2 mb-3">
            <div class="card text-white bg-dark h-100">
                <div class="card-body p-2 p-sm-3">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <div class="text-white-50 small">Hazır Matbaa</div>
                            <div class="fs-5 fs-md-4 fw-semibold">@Model.ToplamHazirMatbaa</div>
                        </div>
                        <div class="fs-3 fs-md-2">🎨</div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Arama Sonuç Bilgisi -->
    @if (!string.IsNullOrEmpty(Context.Request.Query["search"]))
    {
        <div class="row mb-3">
            <div class="col-12">
                <div class="alert alert-info">
                    <div class="d-flex align-items-center">
                        <i class="bi bi-search me-2"></i>
                        <div>
                            Arama sonucu: "<strong>@Context.Request.Query["search"]</strong>" -
                            <strong>@Model.TumUrunDurumlari.Count</strong> kayıt bulundu
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }

    <!-- Tüm Ürünlerin Mevcut Durumu - MOBILE OPTIMIZED -->
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <div class="d-flex flex-column flex-md-row justify-content-between align-items-start align-items-md-center">
                        <h5 class="mb-2 mb-md-0">📊 Tüm Ürünler - Mevcut Bölümleri</h5>
                        <span class="badge bg-light text-dark fs-6">@Model.TumUrunDurumlari.Count kayıt</span>
                    </div>
                </div>
                <div class="card-body p-0">
                    @if (Model.TumUrunDurumlari.Any())
                    {
                        <div class="table-responsive" style="max-height: 60vh; overflow-y: auto;">
                            <table class="table table-striped table-hover mb-0">
                                <thead class="table-dark sticky-top">
                                    <tr>
                                        <th>Sipariş No</th>
                                        <th>Ürün Adı</th>
                                        <th>Bölüm</th>
                                        <th>Durum</th>
                                        <th>Başlama</th>
                                        <th>Süre</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var urun in Model.TumUrunDurumlari)
                                    {
                                        <tr>
                                            <td><strong class="text-nowrap">@urun.SiparisNo</strong></td>
                                            <td class="text-nowrap">@urun.UrunAdi</td>
                                            <td>
                                                <span class="badge bg-@urun.RenkKodu">@urun.MevcutBolum</span>
                                            </td>
                                            <td>
                                                <span class="badge bg-success">@urun.Durum</span>
                                            </td>
                                            <td class="text-nowrap">
                                                @if (urun.BaslamaTarihi.HasValue)
                                                {
                                                    @urun.BaslamaTarihi.Value.ToString("dd/MM HH:mm")
                                                }
                                                else
                                                {
                                                    @:"-"
                                                }
                                            </td>
                                            <td class="text-nowrap">
                                                @if (urun.BaslamaTarihi.HasValue)
                                                {
                                                    var gecenSure = DateTime.Now - urun.BaslamaTarihi.Value;
                                                    var toplamSaat = (int)gecenSure.TotalHours;
                                                    var toplamGun = (int)gecenSure.TotalDays;

                                                    if (toplamGun > 0)
                                                    {
                                                        <span class="badge bg-warning">@toplamGun gün</span>
                                                    }
                                                    else if (toplamSaat > 0)
                                                    {
                                                        <span class="badge bg-info">@toplamSaat sa</span>
                                                    }
                                                    else
                                                    {
                                                        <span class="badge bg-secondary">@((int)gecenSure.TotalMinutes) dk</span>
                                                    }
                                                }
                                                else
                                                {
                                                    <span class="badge bg-secondary">-</span>
                                                }
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                    else
                    {
                        <div class="alert alert-info m-3">
                            <div class="d-flex align-items-center">
                                <i class="bi bi-info-circle me-2"></i>
                                <span>📝 Sistemde aktif üretim bulunmamaktadır.</span>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>