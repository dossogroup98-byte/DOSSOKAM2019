@model RaporViewModel
@{
    Layout = "_Layout";
    ViewData["Title"] = $"{Model.RaporTipi} Rapor";
}

<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center">
                <h2>📈 @Model.RaporTipi Üretim Raporu</h2>
                <a href="@Url.Action("ExcelAktar", "Rapor", new { 
                    raporTipi = Model.RaporTipi, 
                    baslangic = Model.BaslangicTarihi, 
                    bitis = Model.BitisTarihi 
                })" class="btn btn-success">
                    📊 Excel'e Aktar
                </a>
            </div>
            <div class="alert alert-info">
                <strong>Tarih Aralığı:</strong> @Model.BaslangicTarihi.ToString("dd.MM.yyyy") - @Model.BitisTarihi.ToString("dd.MM.yyyy") |
                <strong>Oluşturulma:</strong> @Model.OlusturmaTarihi.ToString("dd.MM.yyyy HH:mm")
            </div>
        </div>
    </div>

    <!-- Makine Saat Raporu -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="card">
                <div class="card-header bg-danger text-white">
                    <h5 class="mb-0">🏭 Makine Saat Raporu</h5>
                </div>
                <div class="card-body">
                    @if (Model.MakineSaatRapor.Any())
                    {
                        <div class="table-responsive">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th>Ürün Adı</th>
                                        <th>Tamamlanan</th>
                                        <th>Devam Eden</th>
                                        <th>Toplam</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model.MakineSaatRapor)
                                    {
                                        <tr>
                                            <td>@item.UrunAdi</td>
                                            <td><span class="badge bg-success">@item.Tamamlanan</span></td>
                                            <td><span class="badge bg-warning">@item.DevamEden</span></td>
                                            <td><span class="badge bg-primary">@item.Toplam</span></td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                    else
                    {
                        <div class="alert alert-warning">
                            Bu tarih aralığında Makine Saat kaydı bulunmamaktadır.
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>

    <!-- Ekleme Raporu -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="card">
                <div class="card-header bg-warning text-white">
                    <h5 class="mb-0">🔧 Ekleme Raporu</h5>
                </div>
                <div class="card-body">
                    @if (Model.EklemeRapor.Any())
                    {
                        <div class="table-responsive">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th>Ürün Adı</th>
                                        <th>Tamamlanan</th>
                                        <th>Devam Eden</th>
                                        <th>Toplam</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model.EklemeRapor)
                                    {
                                        <tr>
                                            <td>@item.UrunAdi</td>
                                            <td><span class="badge bg-success">@item.Tamamlanan</span></td>
                                            <td><span class="badge bg-warning">@item.DevamEden</span></td>
                                            <td><span class="badge bg-primary">@item.Toplam</span></td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                    else
                    {
                        <div class="alert alert-warning">
                            Bu tarih aralığında Ekleme kaydı bulunmamaktadır.
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>

    <!-- Dazmal Raporu -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="card">
                <div class="card-header bg-info text-white">
                    <h5 class="mb-0">⚡ Dazmal Raporu</h5>
                </div>
                <div class="card-body">
                    @if (Model.DazmalRapor.Any())
                    {
                        <div class="table-responsive">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th>Ürün Adı</th>
                                        <th>Tamamlanan</th>
                                        <th>Devam Eden</th>
                                        <th>Toplam</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model.DazmalRapor)
                                    {
                                        <tr>
                                            <td>@item.UrunAdi</td>
                                            <td><span class="badge bg-success">@item.Tamamlanan</span></td>
                                            <td><span class="badge bg-warning">@item.DevamEden</span></td>
                                            <td><span class="badge bg-primary">@item.Toplam</span></td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                    else
                    {
                        <div class="alert alert-warning">
                            Bu tarih aralığında Dazmal kaydı bulunmamaktadır.
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>

    <!-- Diğer bölümler için aynı şablonu kullan -->
    <!-- Kesme, Paketleme, Hazır Dokuma, Hazır Matbaa -->

    <div class="row mt-4">
        <div class="col-12 text-center">
            <a href="@Url.Action("Index", "Rapor")" class="btn btn-secondary">← Rapor Sayfasına Dön</a>
        </div>
    </div>
</div>
