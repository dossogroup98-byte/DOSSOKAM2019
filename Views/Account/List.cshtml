@model List<Kullanici>
@{
    ViewData["Title"] = "Kullanıcı Listesi";
}

<div class="container-fluid">
    <!-- DATABASE SWITCHER - YENİ EKLENDİ -->
    <div class="card mb-3">
        <div class="card-body py-2">
            <div class="row align-items-center">
                <div class="col-md-6 mb-2 mb-md-0">
                    <small class="text-muted">🗃️ Aktif Veritabanı:</small>
                    <strong class="text-primary">@(ViewBag.SelectedDatabase == "postgresql" ? "PostgreSQL" : "SQL Server")</strong>
                </div>
                <div class="col-md-6 text-md-end">
                    <form method="post" action="@Url.Action("SwitchDatabase", "Account")" class="d-inline">
                        <div class="btn-group btn-group-sm" role="group">
                            <button type="submit" name="database" value="sqlserver"
                                    class="btn @(ViewBag.SelectedDatabase == "sqlserver" ? "btn-primary" : "btn-outline-primary")">
                                📊 SQL Server
                            </button>
                            <button type="submit" name="database" value="postgresql"
                                    class="btn @(ViewBag.SelectedDatabase == "postgresql" ? "btn-success" : "btn-outline-success")">
                                🐘 PostgreSQL
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="row mb-4">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center">
                <h2>👥 Kullanıcı Listesi</h2>
                <a href="@Url.Action("Create", "Account", new { database = ViewBag.SelectedDatabase })" class="btn btn-success">➕ Yeni Kullanıcı</a>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body p-0">
                    @if (Model.Any())
                    {
                        <div class="table-responsive">
                            <table class="table table-striped table-hover mb-0">
                                <thead class="table-dark">
                                    <tr>
                                        <th>Ad Soyad</th>
                                        <th>Kullanıcı Adı</th>
                                        <th>Rol</th>
                                        <th>Durum</th>
                                        <th>Kayıt Tarihi</th>
                                        <th>İşlemler</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var kullanici in Model)
                                    {
                                        <tr>
                                            <td><strong>@kullanici.AdSoyad</strong></td>
                                            <td>@kullanici.KullaniciAdi</td>
                                            <td>
                                                <span class="badge @(kullanici.Role == "Yönetici" ? "bg-danger" : kullanici.Role == "Kullanıcı" ? "bg-primary" : "bg-secondary")">
                                                    @kullanici.Role
                                                </span>
                                            </td>
                                            <td>
                                                @if (kullanici.Aktif)
                                                {
                                                    <span class="badge bg-success">🟢 Aktif</span>
                                                }
                                                else
                                                {
                                                    <span class="badge bg-danger">🔴 Pasif</span>
                                                }
                                            </td>
                                            <td>@kullanici.KayitTarihi.ToString("dd.MM.yyyy HH:mm")</td>
                                            <td>
                                                <form method="post" asp-action="Delete" class="d-inline">
                                                    <input type="hidden" name="id" value="@kullanici.KullaniciID" />
                                                    <input type="hidden" name="database" value="@ViewBag.SelectedDatabase" />
                                                    <button type="submit" class="btn btn-danger btn-sm"
                                                            onclick="return confirm('@kullanici.AdSoyad kullanıcısını silmek istediğinize emin misiniz?')">
                                                        🗑️ Sil
                                                    </button>
                                                </form>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                    else
                    {
                        <div class="alert alert-info m-3 text-center">
                            <h5>📝 Henüz kullanıcı bulunmamaktadır</h5>
                            <p>Yeni kullanıcı eklemek için "Yeni Kullanıcı" butonuna tıklayın.</p>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>