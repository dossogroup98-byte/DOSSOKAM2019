@model MakineSaat
@{
    Layout = "_Layout";
    ViewData["Title"] = "Yeni Makine Saati Kaydı";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-md-8 mx-auto">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h4>➕ Yeni Makine Saati Kaydı</h4>
                </div>
                <div class="card-body">
                    <form asp-action="Create">
                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-group mb-3">
                                    <label asp-for="SiparisNo" class="form-label">Sipariş No *</label>
                                    <input asp-for="SiparisNo" class="form-control" />
                                    <span asp-validation-for="SiparisNo" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-group mb-3">
                                    <label asp-for="UrunAdi" class="form-label">Ürün Adı *</label>
                                    <input asp-for="UrunAdi" class="form-control" />
                                    <span asp-validation-for="UrunAdi" class="text-danger"></span>
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-4">
                                <div class="form-group mb-3">
                                    <label asp-for="KalanSaat" class="form-label">Kalan Saat *</label>
                                    <input asp-for="KalanSaat" type="number" step="0.1" class="form-control" />
                                    <span asp-validation-for="KalanSaat" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="form-group mb-3">
                                    <label asp-for="SiparisMiktar" class="form-label">Sipariş Miktarı *</label>
                                    <input asp-for="SiparisMiktar" type="number" class="form-control" />
                                    <span asp-validation-for="SiparisMiktar" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="form-group mb-3">
                                    <label asp-for="BaslamaTarihi" class="form-label">Başlama Tarihi</label>
                                    <input asp-for="BaslamaTarihi" type="datetime-local" class="form-control" />
                                </div>
                            </div>
                        </div>

                        <!-- ✅ BİTİŞ TARİHİ EKLENDİ -->
                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-group mb-3">
                                    <label asp-for="BitisTarihi" class="form-label">Bitiş Tarihi</label>
                                    <input asp-for="BitisTarihi" type="datetime-local" class="form-control" />
                                </div>
                            </div>
                        </div>

                        <!-- ✅ DOKUMACI COMBOBOX'LARI EKLENDİ -->
                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-group mb-3">
                                    <label class="form-label">Dokumacı 1</label>
                                    <select asp-for="Dokumaci1ID" class="form-control">
                                        <option value="">-- Dokumacı Seçin --</option>
                                        @foreach (var dokumaci in ViewBag.Dokumacilar as List<Dokumaci>)
                                        {
                                            <option value="@dokumaci.DokumaciID">@dokumaci.DokumaciAdi</option>
                                        }
                                    </select>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-group mb-3">
                                    <label class="form-label">Dokumacı 2</label>
                                    <select asp-for="Dokumaci2ID" class="form-control">
                                        <option value="">-- Dokumacı Seçin --</option>
                                        @foreach (var dokumaci in ViewBag.Dokumacilar as List<Dokumaci>)
                                        {
                                            <option value="@dokumaci.DokumaciID">@dokumaci.DokumaciAdi</option>
                                        }
                                    </select>
                                </div>
                            </div>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="Aciklama" class="form-label">Açıklama</label>
                            <textarea asp-for="Aciklama" class="form-control" rows="3"></textarea>
                        </div>
                        <div class="form-group">
                            <button type="submit" class="btn btn-success">💾 Kaydet</button>
                            <a href="@Url.Action("Index")" class="btn btn-secondary">← Listeye Dön</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
